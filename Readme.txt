1. The run_analysis.ipynb:
    A notebook file presenting the pipeline on the full dataset

2. Excute test_analysis.py:
    a. Test the preliminary analysis pipeline on a small sample (6 users)
    b. Output the 1) resampled data (.csv), 2) features created for each user (.csv), 3) the kmeans clustering result: users feautres data with group label (.csv)